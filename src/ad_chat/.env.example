# ===================================================================
# ad_chat 환경 변수 예제
# ===================================================================

# OpenAI API 키 (필수)
OPENAI_API_KEY=sk-your-api-key-here

# ===================================================================
# MCP 서버 설정
# ===================================================================
MCP_SERVER_URL=http://34.44.205.198:3000
MCP_TIMEOUT=30
MCP_MAX_RETRIES=3

# ===================================================================
# MongoDB 설정 (선택 사항 - 없으면 파일 기반 저장소 사용)
# ===================================================================
MONGO_URI=mongodb://localhost:27017/
MONGO_DB_NAME=ai_ad_generator

# ===================================================================
# 작업 상태 폴링 설정
# ===================================================================
POLLING_INTERVAL=10
POLLING_MAX_ATTEMPTS=180

# ===================================================================
# OpenAI 모델 설정
# ===================================================================
OPENAI_MODEL=gpt-5-mini
OPENAI_TEMPERATURE=1.0
OPENAI_MAX_COMPLETION_TOKENS=128000

# ===================================================================
# 파일 경로 설정 (Docker/로컬 환경 자동 감지)
# ===================================================================
# 로컬 환경 (기본값)
RUNTIME_ENV=local
# 경로는 자동 설정: PROJECT_ROOT/src/nanoCocoa_aiserver/static

# Docker 환경 (컨테이너 내부)
# RUNTIME_ENV=docker
# STATIC_BASE_PATH=/app/static

# ===================================================================
# 사용 예시
# ===================================================================
# 1. 로컬 개발:
#    - RUNTIME_ENV=local (또는 미설정)
#    - 경로: /home/user/project/src/nanoCocoa_aiserver/static
#
# 2. Docker 컨테이너:
#    - RUNTIME_ENV=docker
#    - STATIC_BASE_PATH=/app/static
#    - nanoCocoa_aiserver와 볼륨 공유 필요
