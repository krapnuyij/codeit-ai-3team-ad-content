# nanoCocoa MCP Server 의존성 (FastAPI REST API 버전)
# aiserver의 무거운 의존성(torch, diffusers 등)은 제외

# Core dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.2

# FastMCP는 더 이상 필요 없음 (REST API로 전환)
# mcp>=1.0.0
python-dotenv>=1.0.0

# HTTP Client
httpx>=0.27.0

# Utilities
pillow>=11.0.0
numpy>=1.26.4
helper-dev-utils>=0.5.0
